# GUI Framework (Client & Server Tray)
PyQt6

# Time Synchronization (Server - optional, has fallback)
ntplib

# PDF Generation (Reports, Paystubs)
reportlab
Pillow  # Required by reportlab for image handling

# Server Framework
flask
flask-cors

# Production WSGI Server (replaces Flask dev server)
waitress

# HTTP Client (Sync Service)
requests

# Timezone Data (required on Windows for zoneinfo)
tzdata

# Colored Console Output (optional, has fallback)
termcolor

# Windows Autostart Setup (Windows only - optional)
# Required only for setup_autostart.py script on Windows
# Install with: pip install pywin32 winshell
# pywin32
# winshell
